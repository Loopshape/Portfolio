
This extension allows to use Typoscript-Templates for the indexed search extension.

There's a typoscript-template included, which activates the extension with "plugin.tx_indexedsearch.customTemplate = 1".

The search-result divides into head, foot and the rows. The page-browser is used, if there are more than one result pages.
